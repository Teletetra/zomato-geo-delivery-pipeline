datasource db { provider = "postgresql"; url = env("NOTIFICATION_DATABASE_URL") }
generator client { provider = "prisma-client-js" }
model Notification { id String @id @default(uuid()); userId String; type String; title String; body String; read Boolean @default(false); createdAt DateTime @default(now()) }
