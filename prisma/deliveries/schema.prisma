datasource db { provider = "postgresql"; url = env("DELIVERY_DATABASE_URL") }
generator client { provider = "prisma-client-js" }
model Delivery { id String @id @default(uuid()); orderId String @unique; partnerId String?; status String; createdAt DateTime @default(now()); updatedAt DateTime @updatedAt }
