datasource db { provider = "postgresql"; url = env("PAYMENT_DATABASE_URL") }
generator client { provider = "prisma-client-js" }
model Payment { id String @id @default(uuid()); orderId String @unique; amount Decimal @db.Decimal(10,2); status String; providerRef String?; createdAt DateTime @default(now()); updatedAt DateTime @updatedAt }
