datasource db { provider = "postgresql"; url = env("AUTH_DATABASE_URL") }
generator client { provider = "prisma-client-js" }
model Credential { id String @id @default(uuid()); email String @unique; passwordHash String; createdAt DateTime @default(now()); updatedAt DateTime @updatedAt }
