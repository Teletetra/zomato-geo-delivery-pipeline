{
  "name": "zomato-backend",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "build": "pnpm -r build",
    "start:dev": "pnpm --parallel --filter './apps/*' start:dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.10",
    "typescript": "^5.7.3"
  }
}
