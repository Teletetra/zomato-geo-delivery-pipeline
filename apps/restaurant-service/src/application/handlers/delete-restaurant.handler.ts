import { CommandHandler, ICommandHandler } from '@nestjs/cqrs'; import { Inject } from '@nestjs/common'; import { DeleteRestaurantCommand } from '../commands/delete-restaurant.command'; import { RESTAURANT_REPOSITORY, IRestaurantRepository } from '../../domain/repositories/restaurant.repository';
@CommandHandler(DeleteRestaurantCommand) export class DeleteRestaurantHandler implements ICommandHandler<DeleteRestaurantCommand>{constructor(@Inject(RESTAURANT_REPOSITORY)private readonly repo:IRestaurantRepository){} async execute(c:DeleteRestaurantCommand){await this.repo.delete(c.id);return {id:c.id,deleted:true};}}
