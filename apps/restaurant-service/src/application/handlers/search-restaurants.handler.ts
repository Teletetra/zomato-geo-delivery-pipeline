import { QueryHandler, IQueryHandler } from '@nestjs/cqrs'; import { Inject } from '@nestjs/common'; import { SearchRestaurantsQuery } from '../queries/search-restaurants.query'; import { RESTAURANT_REPOSITORY, IRestaurantRepository } from '../../domain/repositories/restaurant.repository'; @QueryHandler(SearchRestaurantsQuery) export class SearchRestaurantsHandler implements IQueryHandler<SearchRestaurantsQuery>{constructor(@Inject(RESTAURANT_REPOSITORY)private readonly repo:IRestaurantRepository){} execute(q:SearchRestaurantsQuery){return this.repo.search(q.query);}}
